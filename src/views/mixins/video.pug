mixin video(video)
    div 
        h4
        a(href=`/videos/${video.id}`)=video.title 
        ul 
            li #{video.description}
            li #{video.createdAt}
            li #{video.hashtags}