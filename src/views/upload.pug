extends base.pug

block content 
    h1=pageTitle

    form(method="POST")
        input(name="title", placeholder="Video Title", required, type="text")
        input(type="submit", value="Upload Video")